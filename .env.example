DB_PASSWORD=changeme
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=changeme_minio
SESSION_SECRET=changeme_session_32bytes_minimum
CORS_ORIGIN=*
APP_URL=http://localhost

# Email (SMTP)
# Для разработки Mailhog поднимается автоматически в docker-compose (порт 8025)
# Для production укажите реальный SMTP-сервер и установите SMTP_ENABLED=true
SMTP_ENABLED=false
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@elbrus.local
