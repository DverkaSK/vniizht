DB_PASSWORD=changeme
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=changeme_minio
SESSION_SECRET=changeme_session_32bytes_minimum
# В продакшне — адрес сервера, например http://192.168.1.100
# В dev — не нужен (подставляется * по умолчанию)
CORS_ORIGIN=*
